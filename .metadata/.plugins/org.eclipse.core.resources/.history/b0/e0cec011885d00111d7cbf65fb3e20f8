package com.vishal;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.ConfigurableApplicationContext;

import com.vishal.service.WorkerService;

@SpringBootApplication
public class Application {

	public static void main(String[] args) {
		ConfigurableApplicationContext ctxt = SpringApplication.run(Application.class, args);
		WorkerService serviceObj = ctxt.getBean(WorkerService.class);
		
//		serviceObj.saveWorkerWithAddress(); // parent + child
		
		
//		serviceObj.deleteWorker(1);  // parent + child
		
		
		serviceObj.findWorkerByID(2);  // parent
		//By default only parent will be feteched because a parent can have multiple child in OneToMany relation
		//By default fetch TYPE is LAZY
		//We can change fetchTYPE to EAGER , then child will be also fetched
		
		
		
	}

}
